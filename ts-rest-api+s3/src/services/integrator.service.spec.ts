const SAMPLE_BODY = {
  msg: "My message",
};

/*
describe("Requests", () => {
  it("Should return Hello there", async () => {
    const i = new IntegratorService();
    expect.assertions(1);
    const response = await i.getHelloWorld();
    expect(response.msg).toBe("Hello there!");
  });

  it("Should return sent message", async () => {
    const i = new IntegratorService();
    expect.assertions(1);
    const response = await i.postHelloWorld(SAMPLE_BODY);
    expect(response.yourMessage).toBe(SAMPLE_BODY.msg);
  });  
});

*/
