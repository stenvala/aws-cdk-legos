{
  "name": "python-step-functions",
  "scripts": {
    "clean": "git clean -xdf",
    "deploy": "cd cdk && cdk deploy && cd ..",
    "destroy": "cd cdk && cdk destroy && cd ..",
    "init": "cd cdk && npm install && cd .."
  }
}
