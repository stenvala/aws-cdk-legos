{
  "name": "python-step-functions",
  "scripts": {
    "clean": "git clean -xdf",
    "deploy": "cd cdk && npx cdk deploy && cd ..",
    "destroy": "cd cdk && npx cdk destroy && cd ..",
    "init": "cd cdk && npm install && cd .."
  }
}
